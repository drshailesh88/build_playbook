# First-principles gaps — rooming patterns with NO Mobbin precedent
**Surface:** rooming · **Status:** confirmed negative results across all three sweep modes (by-app A1–A22, by-flow F1–F40, by-pattern P1–P37, 2026-06-11). These are NOT cards — they are explicit gaps. Anything built here is invention guided by the named donor cards, and the corresponding DEC must say so.

| # | Gap | What no app shows | Donor cards to assemble from |
|---|-----|-------------------|------------------------------|
| 1 | **Ops rooming grid (delegate × room × night)** | Named people allocated into named hotel rooms across nights | `assignment-grid-draft-publish.md` (grid + draft/publish + exception legend) + `unit-picker-person-to-slot.md` (person↔slot binding, completeness) + `block-inventory-grid.md` (per-night counts) |
| 2 | **Roommate pairing / preference matching** | Constraint-based people-pairing (gender, seniority, mutual requests, suggested pairs). Explicit negative: pairing queries returned only dating-swipe and entity-reconciliation UIs (P26) | `scarcity-occupancy-guards.md` (displacement warning), `split-and-shared-stays.md` (disclosure language), `delegate-response-capture.md` (mutual-consent capture), 7shifts assignment modal (P26) |
| 3 | **Rooming-list handoff with structured acknowledgment** | A hotel confirming receipt line-by-line ("rooms 401–412 ok, 413 disputed") — only whole-document ack exists (e-sign) | `hotel-handoff-tracking.md` (sent→viewed→signed donor) + `export-builder-async.md` + `approval-queue-inbox.md` (dispute intake) |
| 4 | **Room-block cutoff / attrition lifecycle** | Contracted-block release dates, attrition thresholds, penalty preview, release-back of unsold rooms | `block-cutoff-deadline-timeline.md` (visual grammar) + `capacity-meters-threshold-alerts.md` (threshold alerts) + `cancellation-consequence-preview.md` (consequence preview shape) |
| 5 | **Cascade reallocation composer** | "This change affects 12 delegates + 2 hotels — preview and send" as one flow | `notification-tiers-and-composer.md` (computed audience + recap) + `change-diff-confirmation.md` (the diff that becomes the message) + `conflict-triage-autofix.md` (what changed and why) + Luma's declared side-effect sentence (`delegate-response-capture.md`) |
| 6 | **Waitlist tied to inventory release** | Cancellation frees a bed → next waitlisted delegate auto-offered with expiry | `waitlist-on-capacity.md` + `cancellation-consequence-preview.md` + `delegate-response-capture.md` (tokened offer with expiry) |
| 7 | **Hotel floor-plan / room-number picker** | Spatial unit pickers exist only for aircraft seats and trains | `unit-picker-person-to-slot.md` transfers as assembly (room tiers = types, ✕ = occupied, auto-assign fallback) |
| 8 | **Ops-side split-stay decision** | Deliberately splitting one delegate across two hotels (block exhausted mid-range) with transfer logistics — only traveler-side display exists | `split-and-shared-stays.md` (rendering) + `no-availability-recovery.md` (the trigger state) + `change-diff-confirmation.md` (the proposal) |
| 9 | **Block negotiation/award loop** | Bids → contract → recorded block; Expedia shows bidding but the award→contract step is invisible | `block-sourcing-rfp.md` (RFP data shape + bid lifecycle) |

**Rule for builders:** none of these may be improvised mid-build. Each needs a founder DEC that cites this file, the donor cards, and accepts that the design is first-principles.
