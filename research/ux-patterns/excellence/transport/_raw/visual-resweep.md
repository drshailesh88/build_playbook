# Transport Pattern Library — SCREEN-Level Visual Evidence Re-Sweep

**Date:** 2026-06-11
**Method:** Live Playwright browser session (MCP). Each finding is recorded only after navigating to the page, scrolling to the relevant section, taking a JPEG screenshot, and visually inspecting the rendered image. Evidence class: SCREEN (real product UI visibly rendered on the page — app-UI renders on product pages and annotated help-center screenshots count; flat marketing illustrations do not). This is a reviewer-mandated re-sweep: the original harvest in this directory was vendor-DOC-based and lacked screen-level evidence for interaction-heavy surfaces.

