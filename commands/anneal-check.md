# anneal-check — DEPRECATED

Replaced by `/playbook:qa-status` — reads `.quality/state.json` and reports
the current state (features green/blocked/pending, modules below floor, last
run timestamp).

Historical version preserved at `commands/deprecated/anneal-check.md`.
